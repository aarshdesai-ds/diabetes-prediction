streamlit>=1.31
pandas>=2.0
numpy>=1.24
# Pinned so the committed model.joblib unpickles cleanly in every environment
# (a model pickled by one scikit-learn version may not load in another).
scikit-learn==1.7.2
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
# Pinned: SHAP's KernelExplainer behaviour varies across versions; pin the one
# the app is tested against so local and deployed environments match.
shap==0.51.0
